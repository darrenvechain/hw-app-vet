{
  "name": "@vechain/hw-app-vet",
  "version": "0.2.0",
  "description": "Ledger Hardware Wallet VeChain JavaScript bindings.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p .",
    "lint": "tslint -p .",
    "prepare": "rm -rf dist/; npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/ledgerhq__hw-transport": "^4.21.1",
    "tslint": "^5.16.0",
    "typescript": "^3.4.3"
  },
  "keywords": [],
  "author": "dinn",
  "license": "LGPL-3.0",
  "dependencies": {}
}
